# Private implementation package for f_nlp. Holds the
# per-concern static classes (_Token, _Strip, _Ngram) and the
# shared language policy (_policy). UNlp binds these as the
# public facade — never import from here outside f_nlp.
